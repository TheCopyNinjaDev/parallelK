Starts with wsl or linux
# Install
sudo apt install build-essential libomp-dev
sudo apt install openmpi-bin openmpi-doc libopenmpi-dev
# Compile
mpicc -o main main.c
# Run
mpirun -np 8 ./main
